<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.olx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Olx">
        <activity android:name=".EnterPhoneNumber"></activity>
        <activity android:name=".GetOTP" />
        <activity android:name=".Settings" />
        <activity android:name=".Billing" />
        <activity android:name=".Bikes" />
        <activity android:name=".Properties" />
        <activity android:name=".MobilesSell" />
        <activity android:name=".IncludeSomeDetails" />
        <activity android:name=".Chat_Section.ChatRecyclerView.ChatActivity" />
        <activity android:name=".ShowProductDetailForBike.BikeDetailActivity" />
        <activity android:name=".ShowProductDetail.MobileDetails" />
        <activity android:name=".mobileSection.MobileMainActivity" /> <!-- </application> -->
        <activity android:name=".bikes.BikeMainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>